  host <%= alces.nodename %> {
    hardware ethernet <%= hwaddr %>;
    option host-name "<%= alces.nodename %>.prv.cluster.local";
    option routers <%= alces.hostip %>;
    filename "/pxelinux.0";
    fixed-address <%= fixedaddr %>;
  }
